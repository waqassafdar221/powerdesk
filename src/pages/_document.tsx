// Re-export the root-level pages _document to avoid duplicate document modules
// @ts-ignore - purposely re-exporting the canonical _document from root pages
export { default } from '../../../pages/_document.js';
